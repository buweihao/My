<UserControl x:Class="MyModbus.DataCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyModbus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Command="{Binding OpenDetailCommand}"
            Cursor="Hand" BorderThickness="0" Padding="0">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border x:Name="CardBorder" 
                        Background="#2D2D30" 
                        CornerRadius="8" 
                        BorderBrush="#444" 
                        BorderThickness="1"
                        Width="160" Height="90"
                        Margin="5">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Ellipse Width="10" Height="10" 
                                 HorizontalAlignment="Right" VerticalAlignment="Top" 
                                 Margin="0,8,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#00FF7F"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGood}" Value="False">
                                            <Setter Property="Fill" Value="Gray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>

                        <TextBlock Text="{Binding Value, StringFormat={}{0:F2}}" 
                                   Foreground="White" 
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <Border Grid.Row="1" Background="#3E3E42" CornerRadius="0,0,8,8" Padding="5">
                            <DockPanel>
                                <TextBlock Text="{Binding Config.TagName}" Foreground="#AAA" FontSize="11"/>
                                <TextBlock Text="rpm" Foreground="#888" FontSize="11" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                            </DockPanel>
                        </Border>
                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal"/>
                            <VisualState x:Name="MouseOver">
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetName="CardBorder" 
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#007ACC" Duration="0:0:0.2"/>
                                </Storyboard>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                </Border>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>
